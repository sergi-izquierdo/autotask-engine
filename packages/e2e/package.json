{
  "name": "@autotask/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo 'Run e2e tests with: npm run test:api or npm run test:ui'",
    "test:api": "vitest run",
    "test:ui": "playwright test",
    "e2e": "./scripts/run-smoke.sh",
    "e2e:ui": "./scripts/run-smoke.sh --ui",
    "build": "echo 'No build step for e2e'",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@autotask/core": "*",
    "@playwright/test": "^1.50.0",
    "vitest": "^4.1.0"
  }
}
